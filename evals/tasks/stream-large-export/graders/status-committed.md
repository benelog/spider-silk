---
type: 'llm'
---

PASS if the reply says that once the streamed response has started, a failure can no longer change the status code, so anything that should fail with an error status must fail before the response is returned. FAIL if it does not say this.
