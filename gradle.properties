# The one place the release version is written. spider-silk-gradle-plugin
# reads it from here, and the verifyVersionReferences task fails the build
# when the README, the manual, the Maven parent, or the agent skill disagrees.
version=1.0.0
